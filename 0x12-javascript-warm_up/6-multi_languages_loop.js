#!/usr/bin/node
/**
 *  prints 3 lines by using an array of string and a loop
 */
const threeLine = ['C is fun\nPython is cool\nJavaScript is amazing'];
let idx;
for (idx = 0; idx < threeLine.length; idx++) {
  console.log(threeLine[idx]);
}
